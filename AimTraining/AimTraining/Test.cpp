#define TEST 0
#if TEST == 1

# include <Siv3D.hpp>

void Main()
{
	/*
	// セーブデータの読み込み
	TextReader readSaveData(U"example/SaveDate.txt");

	// 読み込みに失敗したら
	if (!readSaveData)
	{
		// 新規作成
		TextWriter writeSaveData(U"SaveData.txt");
	}
	*/

	while (System::Update())
	{
		
	}
}

#endif